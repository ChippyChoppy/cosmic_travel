<h1>Space Force Missions to date...</h1>

<ul>
<% @missions.each do |mission| %>
    <li><%= link_to mission.name, mission_path(mission.id)%></li>
<% end %>

<h3>Launch a new mission</h3>
<p>
<%= link_to "To boldly go...", new_mission_path %></p>